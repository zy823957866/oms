export interface OmsDatepicker {
    startDate?: number;
    endDate?: number;
    timePicker?: boolean;
    timePickerIncrement?: boolean;
    timePicker24Hour?: boolean;
    timePickerSeconds?: boolean;
};